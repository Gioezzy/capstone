import 'package:flutter/widgets.dart';

// Modern Songket color palette.
class AppColors {
  AppColors._();

  static const Color black = Color(0xFF1A1A1A); // Dark charcoal for better contrast
  static const Color nearBlack = Color(0xFF262626);
  static const Color maroon = Color(0xFF800000); // Songket Maroon
  static const Color gold = Color(0xFFD4AF37); // Songket Gold
  static const Color graphite = Color(0xFF4A4A4A);
  static const Color gray = Color(0xFF9E9E9E);
  static const Color borderGray = Color(0xFFE0E0E0);
  static const Color surfaceGray = Color(0xFFF8F9FA); // Off-white
  static const Color white = Color(0xFFFFFFFF);

  // Reserved for destructive actions only.
  static const Color danger = Color(0xFFD32F2F);
}
